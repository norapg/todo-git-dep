# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main # またはmaster
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install && npm run build # ← ここを npm install に変更！
        env:
          # GitHub Actionsのビルド時に必要な環境変数を記述（直接書いても、GitHubのSecretsに入れてもOK）
          VITE_SUPABASE_URL: https://taylayhxueysickwxihj.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_9xqABc1EN20UY4u3R1ahxA_j3srH3MN
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TODO_GIT_DEP }}
          channelId: live
          projectId: todo-git-dep







